.idea
*.pyc
/build
.venv
package-lock.json
src/python/build
src/python/site

# Security - Never commit private keys
id_rsa
*.pem
*.key

# Local runtime config (user-specific)
config/

# SSH keys directory (runtime secrets, never commit)
ssh/
