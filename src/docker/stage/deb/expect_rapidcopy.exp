#!/usr/bin/expect
set timeout 10
spawn "./install_rapidcopy.sh"

expect {
    timeout {error "ERROR: missing user prompt"; exit 1}
    "User for RapidCopy service"
}
send "user\n"

expect {
    timeout {error "ERROR: timeout"; exit 1}
    eof
}
