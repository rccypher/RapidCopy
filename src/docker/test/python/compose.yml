services:
  tests:
    image: rapidcopy/test/python
    container_name: rapidcopy_test_python
    build:
      context: ../../../../
      dockerfile: src/docker/test/python/Dockerfile
      target: rapidcopy_test_python
    volumes:
      - type: bind
        source: ../../../python
        target: /src
        read_only: true
